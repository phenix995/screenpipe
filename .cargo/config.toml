[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-arg=/NODEFAULTLIB:libucrt.lib", "-C", "link-arg=/DEFAULTLIB:ucrt.lib"]

# Force dynamic runtime for all Windows builds
[target.'cfg(windows)']
rustflags = ["-C", "target-feature=-crt-static"]
